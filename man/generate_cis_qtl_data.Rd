% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_eQTL.R
\name{generate_cis_qtl_data}
\alias{generate_cis_qtl_data}
\title{Generate eQTL Data with Multiple Genetic Architecture Components}
\usage{
generate_cis_qtl_data(
  G,
  h2g = 0.25,
  prop_h2_sparse = 0.5,
  prop_h2_oligogenic = 0.35,
  prop_h2_infinitesimal = 0.15,
  n_sparse = 2,
  n_oligogenic = 5,
  n_inf = 15,
  mixture_props = c(0.75, 0.25),
  sparse_sd = 0.5,
  oligo_sds = c(0.05, 0.15),
  inf_sd = 0.01,
  standardize = TRUE,
  independent = TRUE,
  ld_threshold = 0.1,
  ld_step = 0.1,
  ld_max = 0.5,
  max_attempts = 200,
  seed = NULL
)
}
\arguments{
\item{G}{Standardized genotype matrix (samples x SNPs).}

\item{h2g}{Total SNP heritability (proportion of variance explained by genotyped SNPs).}

\item{prop_h2_sparse}{Proportion of h2g explained by sparse effects.}

\item{prop_h2_oligogenic}{Proportion of h2g explained by oligogenic effects.}

\item{prop_h2_infinitesimal}{Proportion of h2g explained by infinitesimal effects.}

\item{n_sparse}{Number of sparse SNPs.}

\item{n_oligogenic}{Number of oligogenic SNPs to simulate.}

\item{n_inf}{Number of infinitesimal SNPs to simulate. If NULL (default), all remaining SNPs after sparse and oligogenic selection receive infinitesimal effects.}

\item{mixture_props}{Mixture proportions for oligogenic effects (must sum to 1). Default c(0.75, 0.25) means 75% smaller effects, 25% larger effects.}

\item{sparse_sd}{Standard deviation for drawing sparse effects (default 0.5).}

\item{oligo_sds}{Standard deviations for oligogenic mixture components (default c(0.05, 0.15)).}

\item{inf_sd}{Standard deviation for drawing infinitesimal effects (default 0.01).}

\item{standardize}{Logical; if TRUE, the genotype matrix will be standardized.}

\item{independent}{Logical; if TRUE, ensures all sparse and oligogenic SNPs have |r| < ld_threshold with each other (default TRUE).}

\item{ld_threshold}{Numeric; starting maximum allowed absolute correlation between causal variants when independent = TRUE (default 0.10). If constraints cannot be satisfied, the threshold is progressively increased by ld_step.}

\item{ld_step}{Numeric; amount to increase ld_threshold after max_attempts failures (default 0.10).}

\item{ld_max}{Numeric; maximum ld_threshold to try before giving up (default 0.50).}

\item{max_attempts}{Integer; maximum number of attempts per LD threshold level to find SNPs satisfying LD constraints (default 200).}

\item{seed}{Optional seed for reproducibility.}
}
\value{
A list containing the standardized genotype matrix, simulated phenotype,
  combined beta values, indices for each effect component, realized heritability estimates,
  effect size ranges, hierarchy validation results, and causal indices.
}
\description{
This function generates simulated gene expression data
}
